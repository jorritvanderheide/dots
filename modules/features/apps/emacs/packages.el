;; -*- no-byte-compile: t; -*-
;;; packages.el

;; Extra packages beyond the enabled Doom modules go here, e.g.:
;;   (package! some-package)
;; then rebuild the system. See the unstraightened README for pinning caveats.
